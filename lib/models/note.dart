class Note {
  final int id;
  final String title;
  final String content;
  final bool isSync;

  Note(
      {required this.id,
      required this.title,
      required this.content,
      required this.isSync});
}
